# Copy to .env.local and fill in. NEVER commit .env.local.
MODAL_TOKEN_ID=ak-XXXXXXXX
MODAL_TOKEN_SECRET=as-XXXXXXXX
HUGGINGFACE_TOKEN=hf_XXXXXXXX   # write token, only needed to push the model (Phase 6)
